import ModalHeader from './ModalHeader'

const Modal = (props) => {
    return (
        <div className='modal'>
            <div className='modal-content'>
                <ModalHeader />
            </div>
        </div>
    );
};

export default Modal;